[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "agencycore-cli"
dynamic = ["version"]
description = "AgencyCore CLI — manage your CRM, outreach, and more from the terminal"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "AlterityNetwork", email = "dev@agencycore.dev" },
]
keywords = ["crm", "cli", "agencycore", "sales", "pipeline"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business",
]
dependencies = [
    "typer>=0.9",
    "httpx>=0.27",
    "supabase>=2.0",
    "rich>=13.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=6.0",
    "pytest-watch>=4.2",
    "respx>=0.22",
]

[project.urls]
Homepage = "https://github.com/AlterityNetwork/ac-cli"
Repository = "https://github.com/AlterityNetwork/ac-cli"
Issues = "https://github.com/AlterityNetwork/ac-cli/issues"

[tool.hatch.version]
source = "vcs"
raw-options = { root = "." }

[tool.hatch.build.hooks.vcs]
version-file = "src/ac_cli/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/ac_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=src/ac_cli --cov-report=term-missing:skip-covered"

[project.scripts]
ac = "ac_cli.main:app"
