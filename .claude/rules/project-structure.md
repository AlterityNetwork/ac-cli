# Project Structure

```
ac-cli/
├── src/ac_cli/
│   ├── __init__.py          # Exports __version__ from _version.py
│   ├── _version.py          # Auto-generated by hatch-vcs (gitignored, do not edit)
│   ├── main.py              # Entry point, registers all sub-apps, --version flag
│   ├── client.py            # Authenticated httpx client from stored config
│   ├── config.py            # Load/save ~/.agencycore/config.json, environment constants (STAGING_*, DEV_*)
│   ├── formatting.py        # Shared output: print_table, print_detail, print_json
│   └── commands/
│       ├── _helpers.py        # Shared helpers: _api_request, _build_body, _handle_error
│       ├── auth.py           # login, logout, whoami
│       ├── health.py         # health check (no auth)
│       ├── writing_styles.py  # styles app: list, get, create, update, delete, train, feedback, iterate, analyze
│       ├── apps.py           # apps app: install, uninstall, list, usage-event, usage, configs, update-config, delete-config
│       ├── nylas.py          # nylas app: oauth-start, account, org-accounts, disconnect, send, update-signature, validate-signature
│       ├── hooks.py          # hooks app: list
│       ├── crm/              # CRM subcommands (package)
│       │   ├── __init__.py    # app, _CRM prefix, search, dashboard (imports helpers from _helpers.py)
│       │   ├── companies.py   # companies_app: list, get, create, update, delete
│       │   ├── people.py      # people_app: list, get, create, update, delete
│       │   ├── deals.py       # deals_app: list, get, create, update, move, order, delete
│       │   ├── activities.py  # activities_app: list, get, create, update, complete, delete
│       │   ├── communications.py  # communications_app: list, get, thread, unread, mark-read, delete, delete-thread, update, archive, unarchive, contact-by-email, resolve-contact, draft-email, generate-draft, unread-thread-ids
│       │   ├── lists.py       # lists_app: list, get, create, update, members, add-member, remove-member, delete
│       │   └── imports.py     # imports_app: preview, commit
│       ├── envoy/            # Envoy outreach subcommands (package)
│       │   ├── __init__.py    # app, _ENVOY prefix, envoy_callback, registers sub-apps + signals + inbox-count
│       │   ├── sequences.py   # sequences_app: list, get, create, update, delete, launch, pause, resume
│       │   ├── steps.py       # steps_app: create, update, delete, reorder, stats
│       │   ├── recipients.py  # recipients_app: list, add, remove
│       │   ├── outbox.py      # outbox_app: pending, sent, step-drafts, update-draft, approve, reject, regenerate
│       │   ├── playbooks.py   # playbooks_app: list, get, create, update, delete, duplicate
│       │   ├── battlecards.py # battlecards_app: list, get, create, update, delete, duplicate
│       │   ├── inbox.py       # inbox_app: list, messages, archive, unarchive, assign, snooze, complete, update-status, add-tags, remove-tags, reply
│       │   ├── signals.py     # signals_command (standalone command)
│       │   └── dashboard.py   # dashboard_command (standalone command)
│       ├── workflows/        # Workflow subcommands (package)
│       │   ├── __init__.py    # app, _WORKFLOWS prefix, registers sub-apps
│       │   ├── runs.py        # runs_app: create, list, get, logs
│       │   ├── schedules.py   # schedules_app: list, get, create, update, delete, preview, toggle
│       │   └── presets.py     # presets_app: list, get, create, update, delete
│       └── admin/            # Admin subcommands (package, super admin only)
│           ├── __init__.py    # app, _ADMIN prefix, registers sub-apps
│           ├── users.py       # users_app: list, get, create, update, delete, search, reset-password, impersonate, exit-impersonation, generate-link
│           ├── organizations.py # organizations_app: list, get, create, update, delete, members, add-member, update-member, remove-member, transfer-ownership
│           ├── queues.py      # queues_app: health, stats, queue-stats, metrics, send-to-sentry, job-performance, failed, retry-all, retry-job, clear-failed
│           └── demo.py        # demo_app: scrape-website, generate-org, generate-profile, prepare-account, list-accounts, get-account, update-account, delete-account, cleanup, stats
├── tests/
│   ├── conftest.py           # Shared fixtures (mock_api, mock_config, invoke)
│   ├── test_formatting.py
│   ├── test_crm_search.py
│   ├── test_crm_companies.py
│   ├── test_crm_people.py
│   ├── test_crm_deals.py
│   ├── test_crm_activities.py
│   ├── test_crm_communications.py
│   ├── test_crm_dashboard.py
│   ├── test_crm_lists.py
│   ├── test_crm_imports.py
│   ├── test_envoy_sequences.py
│   ├── test_envoy_steps.py
│   ├── test_envoy_recipients.py
│   ├── test_envoy_outbox.py
│   ├── test_envoy_dashboard.py
│   ├── test_envoy_playbooks.py
│   ├── test_envoy_battlecards.py
│   ├── test_envoy_inbox.py
│   ├── test_envoy_signals.py
│   ├── test_writing_styles.py
│   ├── test_workflows_runs.py
│   ├── test_workflows_schedules.py
│   ├── test_workflows_presets.py
│   ├── test_apps.py
│   ├── test_admin_users.py
│   ├── test_admin_organizations.py
│   ├── test_admin_queues.py
│   ├── test_admin_demo.py
│   ├── test_nylas.py
│   ├── test_hooks.py
│   └── test_token_refresh.py
├── scripts/
│   ├── bump.sh              # Manual version bump: ./scripts/bump.sh patch|minor|major [--push]
│   └── auto-version-tag.sh  # Post-commit hook: auto-tags based on conventional commit prefix
├── pyproject.toml
└── uv.lock
```

## Adding New CRM Subcommand Groups
1. Create `src/ac_cli/commands/crm/<group>.py` with `<group>_app = typer.Typer(help="...")`
2. Import shared helpers: `from ac_cli.commands._helpers import _api_request, _build_body`
3. Import CRM prefix: `from ac_cli.commands.crm import _CRM`
4. Add `ctx: typer.Context` as first parameter to commands that need `--json` output
5. Use `_api_request("get", ...)` for API calls and `_build_body(...)` for request bodies
6. Read JSON flag via `ctx.obj["json"]`
7. Register in `crm/__init__.py`: import the app and call `app.add_typer(<group>_app, name="<group>")`

## Adding Envoy Subcommand Groups
1. Create `src/ac_cli/commands/envoy/<group>.py` with `<group>_app = typer.Typer(help="...")`
2. Import shared helpers: `from ac_cli.commands._helpers import _api_request, _build_body`
3. Import envoy prefix: `from ac_cli.commands.envoy import _ENVOY`
4. Add `ctx: typer.Context` as first parameter to commands that need `--json` output
5. Read JSON flag via `ctx.obj["json"]`
6. Register in `envoy/__init__.py`: import the app and call `app.add_typer(<group>_app, name="<group>")`

## Adding Non-CRM/Envoy Command Groups
1. Create `src/ac_cli/commands/<group>.py` (or package) with `app = typer.Typer(help="...")`
2. Import and register in `main.py`: `app.add_typer(<group>.app, name="<group>")`

## API Endpoints
- CRM commands hit `/api/v1/crm/*` — the `_CRM` constant in `crm/__init__.py` holds this prefix
- Envoy commands hit `/api/v1/envoy/*` — the `_ENVOY` constant in `envoy/__init__.py` holds this prefix
- All domains share `_api_request()`, `_build_body()`, and `_handle_error()` from `commands/_helpers.py`
- The API base URL is stored in `~/.agencycore/config.json` (set during `ac login`). Environment constants live in `config.py`: `STAGING_*` (default) and `DEV_*` (used with `ac login --dev`).
